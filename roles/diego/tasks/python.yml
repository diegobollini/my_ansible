---
- name: Python3 - Instalar dependencias y paquetes
  ansible.builtin.apt:
    name: "{{ packages }}"
    state: present
  vars:
    packages:
      - python3
      - ipython3
      - python3-setuptools
      - python3-pip
      - pylint
  tags: python3
